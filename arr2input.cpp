#include<iostream>
using namespace std;
int main(){
    int arrays[5];
    for(int idx=0;idx<5;idx++){
        cin>>arrays[idx];
    }
    for(int idx=0;idx<5;idx++){
        cout<<arrays[idx]<<endl;
    }
    return 0;
}
